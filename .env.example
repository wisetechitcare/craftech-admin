# Base URL for the backend API.
# Leave as /api in development (Vite proxies /api -> http://localhost:5000, see vite.config.ts).
# In production set this to the deployed API origin, e.g. https://api.craftech.com/api
VITE_API_URL=/api
