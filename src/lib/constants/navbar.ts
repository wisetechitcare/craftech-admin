/** Optional header parts from the visibility registry (`navbar` group). */
export enum NavbarVisibilityKey {
  THEME_TOGGLE = "navbar.themeToggle",
  CTA = "navbar.cta",
}

export const NAVBAR_VISIBILITY_GROUP = "navbar";
